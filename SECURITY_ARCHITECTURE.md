.# 🛡️ NCTB World-Class Enterprise Security & DRM Architecture

এই ডকুমেন্টটি অ্যাপ্লিকেশনের সমস্ত ডেটা, পিডিএফ কন্টেন্ট এবং ইউআইকে অবৈধ অ্যাক্সেস, স্ক্রিনশট, স্ক্রিন রেকর্ডিং, পাইরেসি, রিভার্স ইঞ্জিনিয়ারিং এবং রুট/হুকিং বাইপাস থেকে ১০০% সুরক্ষিত রাখার গাইডলাইন।

---

## 🌟 ১. সিকিউরিটি লেয়ার ও সুরক্ষা ব্যবস্থা (Core Security Pillars)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     🛡️ ৫-স্তরের সামরিক-গ্রেড সুরক্ষা                    │
├─────────────────────────────────────────────────────────────────────────┤
│  ১. স্ক্রিন গার্ড   │ WindowManager FLAG_SECURE + রিয়েল-টাইম কাস্টিং ব্লক   │
│  ২. পিডিএফ ডিআরএম  │ AES-256 GCM এনক্রিপশন + র্যাম-বেসড সিকিউর স্ট্রিমিং     │
│  ৩. রুট/হুকিং শিল্ড │ Frida, Xposed, Magisk, Su বাইনারি ও এমুলেটর ডিটেকশন     │
│  ৪. নেটওয়ার্ক শিল্ড  │ SSL Pinning + Cleartext Traffic ব্লক + ডাইনামিক টোকেন   │
│  ৫. কোড প্রটেকশন   │ R8 Full Obfuscation + ProGuard স্ট্রিং এনক্রিপশন       │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔒 ২. বিস্তারিত সুরক্ষা কর্মপদ্ধতি

### ক. স্ক্রিনশট ও স্ক্রিন রেকর্ডিং সম্পূর্ণ প্রতিরোধ (Zero Screenshot Policy):
* **সিস্টেম ফ্ল্যাগ:** `MainActivity`-তে `FLAG_SECURE` সক্রিয় করা হয়েছে। 
* **ফলাফল:** সাধারণ স্ক্রিনশট, থার্ড পার্টি অ্যাপ, রিসেন্ট অ্যাপস প্রিভিউ বা যেকোনো স্ক্রিন রেকর্ডার চালু করলে স্ক্রিনটি ১০০% কালো (Black Screen) হয়ে যাবে।

### খ. পিডিএফ কন্টেন্ট নিরাপত্তা ও পাইরেসি প্রতিরোধ (PDF DRM & Anti-Leech):
1. **মেমোরি স্ট্রিমিং (In-Memory Only):**
   * পিডিএফ ফাইল কখনোই ডিভাইসের পাবলিক স্টোরেজে (`Download`, `SD Card`) সেভ হয় না।
   * ফাইলগুলো অ্যাপের এনক্রিপ্টেড ইন্টারনাল ক্যাশে (`context.cacheDir`) ক্ষণস্থায়ীভাবে রেন্ডার হয় এবং অ্যাপ বন্ধ হওয়ার সাথে সাথে মেমোরি স্বয়ংক্রিয়ভাবে জিরো/শ্রেড (Secure Shredding) হয়ে যায়।
2. **এনক্রিপ্টেড পাইপলাইন (AES-256 GCM):**
   * ব্যাকএন্ড থেকে সরাসরি ইন-মেমোরি বাইটস্ট্রিম ডিকোড করে `PdfRenderer`-এ প্রজেক্ট করা হয়, ফলে কোনো স্নাইফার টুল দিয়ে আসল পিডিএফ ডাউনলোড করা সম্ভব নয়।

### গ. অ্যান্টি-হ্যাক ও রুট ডিটেকশন (Root & Hooking Defense):
* অ্যাপ চালুর সময় `SecurityManager` পরীক্ষা করবে:
  * ডিভাইস রুটেড কিনা (`su` binary, Magisk, Superuser)।
  * Frida বা Xposed ফ্রেমওয়ার্ক দিয়ে অ্যাপ মেমোরি ইন্টারসেপ্ট করার চেষ্টা হচ্ছে কিনা।
  * সন্দেহজনক ডিবাগিং বা ডিকম্পাইলার রান হচ্ছে কিনা।
* বিপদ শনাক্ত হলে অ্যাপ ব্যবহারকারীকে সতর্কবার্তা দিয়ে নিরাপদে বন্ধ হয়ে যাবে।

### ঘ. নেটওয়ার্ক সিকিউরিটি কনফিগ (Network Security):
* শুধুমাত্র সিকিউর `HTTPS` এনক্রিপ্টেড ট্রাফিক অনুমোদিত।
* যেকোনো প্রকার প্লেইনটেক্সট `HTTP` ডেটা আদান-প্রদান সিস্টেম লেভেলে নিষিদ্ধ (`cleartextTrafficPermitted="false"`).

---

## ⚙️ ৩. সেন্ট্রালাইজড কনফিগারেশন (`core/config/AppConfig.kt`)

সমস্ত সিকিউরিটি ফিচার এক জায়গা থেকে টগল করার ব্যবস্থা:
* `FLAG_SECURE_ENABLED = true`
* `ROOT_DETECTION_ENABLED = true`
* `TAMPER_DETECTION_ENABLED = true`
* `ENCRYPTED_PDF_STREAMING = true`

---

## 🖥️ ৪. অ্যাডমিন প্যানেল এন্টারপ্রাইজ সিকিউরিটি ও গভর্নেন্স (Admin Panel Security)

```
┌─────────────────────────────────────────────────────────────────────────┐
│               🔐 অ্যাডমিন প্যানেল মাল্টি-লেয়ার প্রটেকশন               │
├─────────────────────────────────────────────────────────────────────────┤
│  ১. অ্যাকশন পিন ভেরিফিকেশন │ বই/অধ্যায় ডিলিট বা বাল্ক আপডেটে মাস্টার পিন চাইবে │
│  ২. ইনঅ্যাক্টিভ অটো-লক      │ ১৫ মিনিট কোনো মাউস/কী-বোর্ড নড়াচড়া না হলে লক     │
│  ৩. ব্রুট-ফোর্স লকআউট     │ ৫ বার ভুল পিন দিলে ৩০ সেকেন্ড সাসপেনশন           │
│  ৪. অ্যাক্টিভিটি অডিট লগ   │ কোন সময়ে কে কী পরিবর্তন/ডিলিট করেছে তার হিস্ট্রি   │
│  ৫. কাস্টম মাস্টার পিন     │ অ্যাডমিন যেকোনো সময় সিকিউরিটি পিন পরিবর্তন করতে পারে │
└─────────────────────────────────────────────────────────────────────────┘
```

